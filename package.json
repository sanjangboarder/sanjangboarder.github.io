{
  "name": "github-blog-premium",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build && node -e \"const fs = require('fs'); if (fs.existsSync('dist/sitemap-index.xml')) { fs.copyFileSync('dist/sitemap-index.xml', 'dist/sitemap.xml'); console.log('Copied sitemap-index.xml to sitemap.xml'); }\" && pagefind --site dist",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^4.0.0",
    "@astrojs/sitemap": "~3.4.0",
    "pagefind": "^1.1.0"
  }
}
